services:
  - type: web
    name: linnyiaung
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
    envVars:
      - key: NODE_OPTIONS
        value: --max-old-space-size=2048
    autoDeploy: true